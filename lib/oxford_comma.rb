def oxford_comma(array)
  if array.length == 1 
    return array.first
  elsif array.length == 2
    array.join(" and ")
  elsif array.length == 3
    array.join
  var x = arr.pop
    arr.join(", ") + ", and" + x
  else array.length > 3
   
  end
end
